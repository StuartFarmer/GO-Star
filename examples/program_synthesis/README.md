# Program Synthesis Example

Goal: write code that determines whether a comma-separated list of integers is strictly
increasing with no duplicates, and prints strict JSON:

```json
{"is_sorted_unique": true}
```

Run:
```bash
uv run dsstar --config examples/program_synthesis/program_config.yaml
```
