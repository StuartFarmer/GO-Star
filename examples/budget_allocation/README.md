# Budget Allocation Example

Goal: output strict JSON with an "allocations" object for categories A, B, C, D.
The evaluator checks constraints and maximizes impact.

Run:
```bash
uv run dsstar --config examples/budget_allocation/budget_config.yaml
```
